import mockData from"./mock-data";export const extractLocations=events=>{const extractedLocations=events.map((event=>event.location));return[...new Set(extractedLocations)]};export const getEvents=async()=>mockData;